# Gradle JVM settings
# Set minimum and maximum heap size to 1GB
org.gradle.jvmargs=-Xms256m -Xmx1024m

# Enable Gradle daemon for faster builds
org.gradle.daemon=true

# Enable parallel builds
org.gradle.parallel=true
